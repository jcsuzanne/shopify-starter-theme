// Import a folder
import.meta.glob(`../css/settings/*.css`, {eager: true});
import.meta.glob(`../css/vendors/*.css`, {eager: true});
import.meta.glob(`../css/base/*.css`, {eager: true});
import.meta.glob(`../css/common/*.css`, {eager: true});
import.meta.glob(`../css/utilities/*.css`, {eager: true});