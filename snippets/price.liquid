{%- assign klass_text = klass | default: 'u-pdp-price' -%}
<p class='{{ klass_text }}'>
  <strike x-text='comparedPriceToDisplay'>
    {{- product.compare_at_price | money -}}
  </strike>
  <span x-text='priceToDisplay'>{{ product.price | money }}</span>
</p>
